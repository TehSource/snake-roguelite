// Script: init_health_component
// Call this on any object to give it health.
// Example: init_health_component(10);

function init_health_component(_hp)
{
    // The amount of health this object has right now.
    hp = _hp;
    
    // The maximum amount of health this object can have.
    max_hp = _hp;
}